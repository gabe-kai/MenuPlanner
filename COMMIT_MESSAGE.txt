Phase 4 – Ingredient Lane, Leftovers (Phase 4 starter)

- Branch: feature/phase4-ingredient-lane
- Goal: begin Phase 4 implementation with a functional ingredient-aware planner lane foundation.

What changed
- Added pure ingredient-lane domain helpers in `src/lib/ingredientLane.ts`:
  - `computeLeftoverBatches(...)` to derive `LeftoverBatch[]` from planned meals + recipes
    with expiration behavior
  - `computeIngredientBalance(...)` to derive coverage/shortage by ingredient and unit
  - Added supporting types: `LeftoverBatch`, `IngredientBalance`, `ParsedIngredient`
  - Added parsing and date-key utilities used by these helpers
- Integrated helpers into `src/app/planner/page.tsx`:
  - Replaced the old mock leftover count with real derived leftovers from current planner data
  - Added Leftover Lane UI (ingredient + quantity + expiration + source-derived list)
  - Added Ingredient Balance UI showing required vs available and shortage status
  - Wired memoization for recipes, leftovers, and balance calculation
- Removed placeholder leftover state from `src/stores/plannerStore.ts`:
  - removed `mockLeftoverCount` from state shape and initial state
- Added tests in `tests/ingredient-lane.test.ts`:
  - verifies leftover generation from planned meals
  - verifies expiration filtering for leftovers
  - verifies ingredient balance math including available vs required

Validation
- `npm run lint` ✅
- `npm test -- tests/ingredient-lane.test.ts` ✅
- `npm run lint:tests` (full suite) ✅
- `npm run build` ✅
  - build remains with existing non-blocking warning: `Module not found: Can't resolve <dynamic>` in `src/lib/log.ts`
    from prior Edge-compatible dynamic import pattern
