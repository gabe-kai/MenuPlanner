feat: finish phase 3.8 account self-service and green local validation

- Added `/account` self-service surface with authenticated load/save profile flow and password rotation.
- Added guarded account APIs:
  - `GET /api/auth/me` for authenticated profile snapshot,
  - `PUT /api/auth/me` for display-name updates,
  - `POST /api/auth/me/password` for current-password-required rotation.
- Added `Session`-anchored settings route protection via proxy middleware and header navigation entry.
- Added focused account API and page tests (`tests/account-api-route.test.ts`, `tests/account-page.test.tsx`).
- Added auth/service typing fixes and a lint cleanup pass for strict TS compatibility (`AuthOptions` typing, account service helpers, store strip-helper cleanup).
- Updated ESLint tooling to a runnable configuration with current dependency set and validated:
  - `npm test -- --runInBand`
  - `npm run lint`
  - `npm run build`
